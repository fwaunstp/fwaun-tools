# GUI assets

`icon.png` is a 256x256 solid-color placeholder. Replace it with a real
icon before publishing — `dx bundle` will derive every platform-specific
icon size (macOS `.icns`, Windows `.ico`, Linux PNGs) from this single
source.
